# Production — подставляются при npm run build
# В CI (GitHub Actions) VITE_BASE_URL задаётся в workflow.

# Базовый путь приложения (для GitHub Pages: /master-marks/, для домена в корне: /)
VITE_BASE_URL=/

# Яндекс.Метрика — ID счётчика
VITE_YANDEX_METRIKA_ID=48202565
